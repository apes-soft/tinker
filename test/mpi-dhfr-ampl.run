# Command line arguments to dynamic:
#
# keyFileBaseName NumberOfTimesteps TimeStep(fm) TimeBetweenOuputs(ps) Temp(K) 
# Press(Atms)
#

# Need to take care of slurm
export SLURM_JOBID=

time mpirun -np 1 \
            amplxe-cl -r S1_1 -collect hotspots -- \
            ../bin/dynamic dhfr 100 1.0 1.0 2 300.0 > >(tee -a out-ampl.txt)  2>&1
